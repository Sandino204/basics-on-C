#include <stdio.h>

// global variable
int y = 50;

void main(){
    // variable => datatype varible_name = value
    
    int num = 10;

    // local variable
    
    int x = 10;

    printf("value of x is %d", x);
    printf("value of num is %d", num);
    printf("value of y is %d", y);
}